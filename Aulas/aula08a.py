# Módulos, pacotes e conjuntos de funções

# Comando import importa uma biblioteca ou módulo (ex. import.bebida)
# para importar um item ou funcao especifica (from.bebida.import.pudim)
#
import math
num = int(input('Digite um número: '))
raiz = math.sqrt(num)
print('A raiz de {} é igual a {}.'.format(num, raiz))
